<!DOCTYPE>
<html>
    <head>
        <meta charset="utf-8">
        <title>
        
        {% block title %}
        {% endblock %}

        </title>

        {% block css %}
        {% endblock %}

    </head>
    <body>

        {% if success %}

        <div class="flashMessage success" id="flashMessage">
            {{ success }}
        </div>

        {% endif %}

        {% if fail %}

        <div class="flashMessage fail" id="flashMessage">
            {{ fail }}
        </div>

        {% endif %}
    

        <nav>
            <ul>
                <li><a href="/blogphp/">Accueil</a></li>
            </ul>
        </nav>

        {% block body %}
        {% endblock %}

        <footer>
            <nav>
                <!--tous les liens ici-->
                <!--si le client est admin on affiche un bouton pour passer à la partie admin-->
                {% if admin %}

                <div class="button">
                    <p><a href="/blogphp/admin-posts">Partie Admin</a></p>
                </div>

                {% endif %}
            </nav>
        </footer>

        {% block js %}
        {% endblock %}

    </body>
</html>