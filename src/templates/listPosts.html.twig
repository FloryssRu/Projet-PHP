{% extends "base.html.twig" %}


{% block title %}
Liste des Posts
{% endblock %}

{% block body %}

<h1>Liste des posts publiés</h1>
{% for post in listPosts %}
    <div class="postInList">
        <h2>{{ post.title }}</h2>
        {% if post.dateLastUpdate %}
            <p>{{ post.dateLastUpdate }}</p>
        {% else %}
            <p>{{ post.datePublication }}</p>
        {% endif %}
        <p>{{ post.heading | raw }}</p>
        <p><a href="post?idPost={{ post.id }}">Lire ce post</a></p>
    </div>
{% endfor %}

{% endblock %}